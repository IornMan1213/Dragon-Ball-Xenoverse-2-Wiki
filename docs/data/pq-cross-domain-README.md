# PQ cross-domain layer

See:

- `parallel-quests-canonical-index.json` — canonical PQ 1-186 batch aggregation.
- `pq-reward-relationships.json` — normalized forward relationship edges.
- `pq-cross-domain-schema.json` — edge schema.
- `pq-cross-domain-audit.json` — completeness gates.
- `pq-cross-domain-completion.md` — exhaustive completion criteria.

This layer is deliberately separate from the quest records so reward relationships can be audited independently.